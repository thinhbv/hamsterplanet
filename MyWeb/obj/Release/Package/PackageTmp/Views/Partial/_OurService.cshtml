@using Libs.Utils
@if (Model.Count > 0)
{
    <!-- our-service-area-start -->
    <div class="our-service-area pt-120">
        <div class="container">
            <div class="section-title mb-60 text-center">
                <h2><span>Dịch vụ</span> của chúng tôi</h2>
            </div>
            <div class="row">
                <div class="carousel-list">
                    @foreach (MyWeb.Entities.Product item in Model)
                    {
                            <div class="single-services pr-20 pb-20">
                                <div class="services-img">
                                    <a href="@StringClass.GeneralDetailUrl(Consts.DICH_VU, item.GroupName, item.Id.ToString(), item.Name)" title="@item.Name"><img src="@StringClass.OriginalImage(item.Image1)" alt="@item.Name" /></a>
                                    <div class="services-title">
                                        <h2><a href="@StringClass.GeneralDetailUrl(Consts.DICH_VU, item.GroupName, item.Id.ToString(), item.Name)">@item.Name</a></h2>
                                    </div>
                                </div>
                            </div>
                            @*<div class="service-wrapper mb-30">
                                    <div class="service-img">
                                        <a href="@StringClass.GeneralDetailUrl(Consts.DICH_VU, item.GroupName, item.Id.ToString(), item.Name)" title="@item.Name"><img src="@StringClass.OriginalImage(item.Image1)" alt="@item.Name" /></a>
                                    </div>
                                    <div class="service-text text-center">
                                        <h2><a href="@StringClass.GeneralDetailUrl(Consts.DICH_VU, item.GroupName, item.Id.ToString(), item.Name)">@item.Name</a></h2>
                                        <p>@StringClass.FormatContentNews(item.Content, 140)</p>
                                    </div>
                                </div>*@

                    }
                </div>
            </div>
        </div>
    </div>
    <!-- our-service-area-end -->
}
